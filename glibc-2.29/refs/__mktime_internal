<def f='codebrowser/time/mktime.c' l='321' ll='517' type='time_t __mktime_internal(struct tm * tp, struct tm *(*)(const time_t *, struct tm *) convert, mktime_offset_t * offset)'/>
<use f='codebrowser/time/mktime.c' l='534' u='c' c='mktime'/>
<doc f='codebrowser/time/mktime.c' l='313'>/* Convert *TP to a time_t value, inverting
   the monotonic and mostly-unit-linear conversion function CONVERT.
   Use *OFFSET to keep track of a guess at the offset of the result,
   compared to what the result would be for UTC without leap seconds.
   If *OFFSET&apos;s guess is correct, only one CONVERT call is needed.
   If successful, set *TP to the canonicalized struct tm;
   otherwise leave *TP alone, return ((time_t) -1) and set errno.
   This function is external because it is used also by timegm.c.  */</doc>
<use f='codebrowser/time/timegm.c' l='33' u='c' c='timegm'/>
