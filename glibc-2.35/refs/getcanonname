<def f='codebrowser/sysdeps/posix/getaddrinfo.c' l='309' ll='324' type='char * getcanonname(nss_action_list nip, struct gaih_addrtuple * at, const char * name)'/>
<use f='codebrowser/sysdeps/posix/getaddrinfo.c' l='861' u='c' c='gaih_inet'/>
<doc f='codebrowser/sysdeps/posix/getaddrinfo.c' l='303'>/* This function is called if a canonical name is requested, but if
   the service function did not provide it.  It tries to obtain the
   name using getcanonname_r from the same service NIP.  If the name
   cannot be canonicalized, return a copy of NAME.  Return NULL on
   memory allocation failure.  The returned string is allocated on the
   heap; the caller has to free it.  */</doc>
